{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"gpu","dataSources":[{"sourceType":"competition","sourceId":129329,"databundleVersionId":15996945},{"sourceType":"modelInstanceVersion","sourceId":732161,"databundleVersionId":15469066,"modelInstanceId":557910,"modelId":570459}],"dockerImageVersionId":31287,"isInternetEnabled":false,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"# 🐦 BirdCLEF 2026: Advanced Training Pipeline [ConvNeXt-V2]\n\nWelcome! Most public notebooks in this competition are **Inference Blends** chasing the public LB. While those are great for your score, they act as \"black boxes\" and don't help you train your own robust models.\n\nThis notebook provides a clean, modular, and highly effective **Training Pipeline** for Sound Event Detection (SED). \n\n### ✨ Key Features:\n1. **Modern Backbone:** `ConvNeXt-V2 Tiny` initialized with FCMAE pre-trained weights.\n2. **GPU Preprocessing:** Audio to Mel-Spectrogram conversion is done on the fly on the GPU using `torchaudio`, heavily speeding up the data pipeline.\n3. **Advanced Pooling:** Generalized Mean (GEM) Pooling over the frequency dimension to highlight bird calls.\n4. **Attention SED Head:** Time-wise attention mechanism to accurately pinpoint short bird calls in a 5-second chunk.\n5. **Robust Augmentations:** Frequency and Time masking (SpecAugment) applied during training.\n6. **Fast Training:** PyTorch Automatic Mixed Precision (AMP) enabled.\n\n*📌 Note: This is the **Train** part of the solution. You can find the corresponding **Inference** notebook with post-processing tricks (Temporal Smoothing & Global Prior) here: [Insert Link to your Inference Notebook]*","metadata":{}},{"cell_type":"code","source":"import os\nimport time\nimport math\nimport random\nimport warnings\nfrom dataclasses import dataclass\nimport glob\n\nimport numpy as np\nimport pandas as pd\nfrom tqdm.auto import tqdm\nfrom sklearn.metrics import roc_auc_score\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import Dataset, DataLoader\nimport torch.cuda.amp as amp\n\nimport torchaudio\nimport torchaudio.transforms as T\nimport timm\n\nwarnings.filterwarnings('ignore')\n\n# ==============================================================================\n# ⚙️ CONFIGURATION\n# ==============================================================================\n@dataclass\nclass CFG:\n    # General\n    seed: int = 42\n    device: str = 'cuda' if torch.cuda.is_available() else 'cpu'\n    output_dir: str = './'\n    \n    # Audio & Spectrogram\n    sr: int = 32000\n    duration: float = 5.0\n    n_mels: int = 128\n    n_fft: int = 2048\n    hop_length: int = 512\n    fmin: int = 20\n    fmax: int = 16000\n    \n    # Model\n    backbone: str = 'convnextv2_tiny.fcmae_ft_in22k_in1k_384'\n    local_weights_dir: str = '/kaggle/input/models/ashishkubade/timm-convnextv2-tiny-fcmae-ft-in22k-in1k-384/pytorch/default/1'\n    num_classes: int = 234\n    in_channels: int = 1 \n    \n    # Training\n    epochs: int = 3\n    batch_size: int = 32\n    num_workers: int = 2\n    lr: float = 5e-5\n    weight_decay: float = 1e-2\n\ndef seed_everything(seed):\n    \"\"\"Ensures absolute reproducibility across all libraries.\"\"\"\n    random.seed(seed)\n    os.environ['PYTHONHASHSEED'] = str(seed)\n    np.random.seed(seed)\n    torch.manual_seed(seed)\n    torch.cuda.manual_seed(seed)\n    torch.backends.cudnn.deterministic = True\n    torch.backends.cudnn.benchmark = False\n    \nseed_everything(CFG.seed)\nprint(f\"Environment setup complete. Using device: {CFG.device}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:24.816614Z","iopub.execute_input":"2026-03-29T16:19:24.816969Z","iopub.status.idle":"2026-03-29T16:19:38.847719Z","shell.execute_reply.started":"2026-03-29T16:19:24.816937Z","shell.execute_reply":"2026-03-29T16:19:38.847012Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 1. Dataset & Data Loading\nWorking with long `.ogg` files can cause memory leaks if not handled properly. Here we use **On-the-fly Random Cropping**:\n- During training, we extract a random 5-second chunk from the audio.\n- If the audio is shorter than 5 seconds, we pad it with zeros.\n- `torchaudio` is used for fast I/O operations directly yielding PyTorch tensors.","metadata":{}},{"cell_type":"code","source":"class BirdDataset(Dataset):\n    def __init__(self, df: pd.DataFrame, cfg: CFG, is_train: bool = True):\n        \n        self.df = df.reset_index(drop=True)\n        self.cfg = cfg\n        self.is_train = is_train\n        self.chunk_samples = int(cfg.sr * cfg.duration)\n\n    def __len__(self):\n        return len(self.df)\n\n    def __getitem__(self, idx):\n        row = self.df.iloc[idx]\n        audio_path = row['filepath']\n        \n        # 1. Load Audio\n        try:\n            waveform, sr = torchaudio.load(audio_path)\n            waveform = torch.nan_to_num(waveform, nan=0.0, posinf=1.0, neginf=-1.0)\n            if sr != self.cfg.sr:\n                waveform = torchaudio.functional.resample(waveform, sr, self.cfg.sr)\n            if waveform.shape[0] > 1:\n                waveform = waveform.mean(dim=0, keepdim=True) # Convert stereo to mono\n        except Exception as e:\n            # Fallback to silence if file is corrupted\n            waveform = torch.zeros(1, self.chunk_samples)\n\n        \n        # 2. Crop or Pad\n        audio_len = waveform.shape[1]\n        \n        if audio_len > self.chunk_samples:\n            if self.is_train:\n                # Random crop for training\n                max_start = audio_len - self.chunk_samples\n                start = np.random.randint(0, max_start)\n            else:\n                # Deterministic crop for validation (first 5 seconds)\n                start = 0 \n            waveform = waveform[:, start:start + self.chunk_samples]\n            \n        elif audio_len < self.chunk_samples:\n            # Pad with zeros if shorter than 5 seconds\n            pad_len = self.chunk_samples - audio_len\n            waveform = F.pad(waveform, (0, pad_len))\n            \n        # 3. Prepare Targets (One-Hot Encoding for BCE Loss)\n        targets = torch.zeros(self.cfg.num_classes, dtype=torch.float32)\n        if 'label_id' in row:\n            targets[row['label_id']] = 1.0\n\n        return waveform, targets\n\ndef prepare_loaders(train_df, valid_df, cfg):\n    train_dataset = BirdDataset(train_df, cfg, is_train=True)\n    valid_dataset = BirdDataset(valid_df, cfg, is_train=False)\n\n    train_loader = DataLoader(\n        train_dataset, batch_size=cfg.batch_size, shuffle=True,\n        num_workers=cfg.num_workers, pin_memory=False\n        , drop_last=True\n    )\n    valid_loader = DataLoader(\n        valid_dataset, batch_size=cfg.batch_size, shuffle=False,\n        num_workers=cfg.num_workers, pin_memory=False, drop_last=False\n    )\n    return train_loader, valid_loader","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.849523Z","iopub.execute_input":"2026-03-29T16:19:38.849937Z","iopub.status.idle":"2026-03-29T16:19:38.859954Z","shell.execute_reply.started":"2026-03-29T16:19:38.849915Z","shell.execute_reply":"2026-03-29T16:19:38.859241Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 2. GPU Audio Processing & SpecAugment\nInstead of pre-computing spectrograms and saving them as images, we calculate them directly inside the network. This allows us to use dynamic `SpecAugment` (time and frequency masking) which drastically reduces overfitting.","metadata":{}},{"cell_type":"code","source":"class AudioTransform(nn.Module):\n    def __init__(self, cfg: CFG, is_train: bool = False):\n        super().__init__()\n        self.mel_spec = T.MelSpectrogram(\n            sample_rate=cfg.sr, n_fft=cfg.n_fft, hop_length=cfg.hop_length,\n            n_mels=cfg.n_mels, f_min=cfg.fmin, f_max=cfg.fmax,\n            norm='slaney', mel_scale='htk'\n        )\n        self.amp_to_db = T.AmplitudeToDB(stype='power', top_db=80)\n        self.is_train = is_train\n        \n        # SpecAugment parameters\n        self.freq_mask = T.FrequencyMasking(freq_mask_param=int(cfg.n_mels * 0.2))\n        self.time_mask = T.TimeMasking(time_mask_param=int((cfg.duration * cfg.sr / cfg.hop_length) * 0.2))\n\n    def forward(self, x):\n        x = self.mel_spec(x)\n        x = self.amp_to_db(x)\n        \n        if self.is_train:\n            x = self.freq_mask(x)\n            x = self.time_mask(x)\n            \n        x = x.to(torch.float32)\n        x = (x - x.mean(dim=[-2, -1], keepdim=True)) / (x.std(dim=[-2, -1], keepdim=True) + 1e-4)\n        \n        # Expand to 3 channels\n        x = x.expand(-1, 3, -1, -1)\n        return x","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.860852Z","iopub.execute_input":"2026-03-29T16:19:38.861142Z","iopub.status.idle":"2026-03-29T16:19:38.887428Z","shell.execute_reply.started":"2026-03-29T16:19:38.861111Z","shell.execute_reply":"2026-03-29T16:19:38.886392Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 3. Model Architecture (SED)\nSound Event Detection (SED) requires knowing *if* a bird is present and roughly *where* in the chunk. \nWe modify `ConvNeXt-V2` by replacing its default Global Average Pooling with **Generalized Mean (GEM) Pooling** over the frequency axis, followed by a time-wise Attention head.","metadata":{}},{"cell_type":"code","source":"class GEMFreqPool(nn.Module):\n    def __init__(self, p_init=3.0, eps=1e-4):\n        super().__init__()\n        self.p = nn.Parameter(torch.tensor(p_init))\n        self.eps = eps\n\n    def forward(self, x):\n        x = x.float()\n        p = self.p.float().clamp(min=1.0, max=10.0)\n        \n        # Apply GEM formula safely\n        x = x.clamp(min=self.eps).pow(p)\n        x = x.mean(dim=2) # Pool over frequency dimension\n        \n        x = x.clamp(min=self.eps).pow(1.0 / p)\n        return x.pow(1.0 / p)\n\nclass BirdModel(nn.Module):\n    def __init__(self, cfg: CFG):\n        super().__init__()\n        \n        weights = glob.glob(f\"{cfg.local_weights_dir}/*.safetensors\") + \\\n                  glob.glob(f\"{cfg.local_weights_dir}/*.bin\") + \\\n                  glob.glob(f\"{cfg.local_weights_dir}/*.pth\")\n        \n        checkpoint_path = weights[0] if weights else ''\n        # 1. Feature Extractor\n        self.backbone = timm.create_model(\n            cfg.backbone, \n            pretrained=False, \n            checkpoint_path=checkpoint_path\n        )\n        in_features = self.backbone.num_features\n        \n        # 2. Pooling Mechanism\n        self.pool = GEMFreqPool()\n        \n        # 3. Attention Classifier Head\n        self.fc = nn.Sequential(\n            nn.Linear(in_features, in_features),\n            nn.ReLU(inplace=True),\n            nn.Dropout(0.2)\n        )\n        self.att_conv = nn.Conv1d(in_features, cfg.num_classes, kernel_size=1)\n        self.cls_conv = nn.Conv1d(in_features, cfg.num_classes, kernel_size=1)\n\n    def forward(self, x):\n        x = self.backbone.forward_features(x)          # (B, C, F, T)\n        x = self.pool(x)              # (B, C, T)\n        \n        x = x.transpose(1, 2)         # (B, T, C)\n        x = self.fc(x)\n        x = x.transpose(1, 2)         # (B, C, T)\n\n        x = x.float()\n        \n        # Time-wise attention\n        att = torch.softmax(self.att_conv(x), dim=-1) \n        cls = self.cls_conv(x)                        \n        \n        # Aggregate over time\n        clipwise_logits = (att * cls).sum(dim=-1)     \n        \n        return {\n            \"clipwise_logits\": clipwise_logits,\n            \"clipwise_probs\": torch.sigmoid(clipwise_logits)\n        }","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.888437Z","iopub.execute_input":"2026-03-29T16:19:38.888805Z","iopub.status.idle":"2026-03-29T16:19:38.901174Z","shell.execute_reply.started":"2026-03-29T16:19:38.888772Z","shell.execute_reply":"2026-03-29T16:19:38.900363Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 4. Training Engine\nWe utilize `BCEWithLogitsLoss` for multi-label classification and enable PyTorch AMP (Automatic Mixed Precision) to dramatically speed up training on modern GPUs without sacrificing accuracy.","metadata":{}},{"cell_type":"code","source":"def train_one_epoch(model, loader, criterion, optimizer, scheduler, scaler, cfg, mel_transform):\n    model.train()\n    total_loss = 0.0\n    valid_batches = 0 # Counter for valid batches\n    pbar = tqdm(loader, desc=\"Training\", leave=False)\n    \n    for waveforms, targets in pbar:\n        waveforms = waveforms.to(cfg.device)\n        targets = targets.to(cfg.device)\n        \n        optimizer.zero_grad()\n        \n        with amp.autocast(enabled=True):\n            mels = mel_transform(waveforms)\n            outputs = model(mels)\n            loss = criterion(outputs['clipwise_logits'], targets)\n        \n        if not math.isfinite(loss.item()):\n            # print(\"WARNING: NaN loss detected. Skipping batch to prevent model corruption.\")\n            optimizer.zero_grad()\n            continue    \n        scaler.scale(loss).backward()\n\n        scaler.unscale_(optimizer)\n        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=2.0)\n        \n        scaler.step(optimizer)\n        scaler.update()\n        \n        total_loss += loss.item()\n        valid_batches += 1\n        pbar.set_postfix(loss=loss.item())\n        \n    scheduler.step()\n    \n    # Avoid division by zero if all batches failed (extremely rare)\n    return total_loss / max(1, valid_batches)\n\n@torch.no_grad()\ndef valid_one_epoch(model, loader, criterion, cfg, mel_transform):\n    model.eval()\n    total_loss = 0.0\n    all_preds, all_targets = [], []\n    pbar = tqdm(loader, desc=\"Validation\", leave=False)\n    \n    for waveforms, targets in pbar:\n        waveforms = waveforms.to(cfg.device)\n        targets = targets.to(cfg.device)\n        \n        mels = mel_transform(waveforms)\n        outputs = model(mels)\n        loss = criterion(outputs['clipwise_logits'], targets)\n        \n        total_loss += loss.item()\n        all_preds.append(outputs['clipwise_probs'].cpu().numpy())\n        all_targets.append(targets.cpu().numpy())\n        \n    all_preds = np.concatenate(all_preds, axis=0)\n    all_targets = np.concatenate(all_targets, axis=0)\n    \n    # Calculate Macro ROC-AUC (Main Competition Metric)\n    def calculate_robust_auc(targets, preds):\n        aucs = []\n        num_classes = targets.shape[1]\n        \n        for i in range(num_classes):\n            # Проверяем, есть ли в классе и 0, и 1\n            if len(np.unique(targets[:, i])) == 2: \n                class_auc = roc_auc_score(targets[:, i], preds[:, i])\n                aucs.append(class_auc)\n                \n        # Усредняем только по \"валидным\" классам\n        if len(aucs) > 0:\n            return np.mean(aucs)\n        return 0.0\n\n    val_auc = calculate_robust_auc(all_targets, all_preds)\n        \n    return total_loss / len(loader), val_auc\n\ndef run_training(train_df, valid_df, cfg):\n    train_loader, valid_loader = prepare_loaders(train_df, valid_df, cfg)\n    \n    model = BirdModel(cfg).to(cfg.device)\n    mel_transform_train = AudioTransform(cfg, is_train=True).to(cfg.device)\n    mel_transform_valid = AudioTransform(cfg, is_train=False).to(cfg.device)\n    \n    criterion = nn.BCEWithLogitsLoss()\n    optimizer = torch.optim.AdamW(model.parameters(), lr=cfg.lr, weight_decay=cfg.weight_decay)\n    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=cfg.epochs, eta_min=1e-6)\n    scaler = amp.GradScaler()\n    \n    best_auc = 0.0\n    print(f\"🚀 Starting training for {cfg.epochs} epochs using {cfg.backbone}\")\n    print(\"=\" * 60)\n    \n    for epoch in range(cfg.epochs):\n        start_time = time.time()\n        \n        train_loss = train_one_epoch(model, train_loader, criterion, optimizer, scheduler, scaler, cfg, mel_transform_train)\n        valid_loss, valid_auc = valid_one_epoch(model, valid_loader, criterion, cfg, mel_transform_valid)\n        \n        elapsed = time.time() - start_time\n        \n        print(f\"Epoch {epoch+1:02d}/{cfg.epochs:02d} | Time: {elapsed:.0f}s | Train Loss: {train_loss:.4f} | Valid Loss: {valid_loss:.4f} | Valid AUC: {valid_auc:.4f}\")\n        \n        if valid_auc > best_auc:\n            best_auc = valid_auc\n            save_path = os.path.join(cfg.output_dir, f\"birdclef26_best_model.pth\")\n            torch.save({\n                'epoch': epoch,\n                'model_state_dict': model.state_dict(),\n                'best_auc': best_auc,\n            }, save_path)\n            print(f\"🌟 Validation AUC improved! Model saved to {save_path}\")\n            \n    print(\"=\" * 60)\n    print(f\"🏁 Training Complete! Best Valid AUC: {best_auc:.4f}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.90223Z","iopub.execute_input":"2026-03-29T16:19:38.902454Z","iopub.status.idle":"2026-03-29T16:19:38.919413Z","shell.execute_reply.started":"2026-03-29T16:19:38.902435Z","shell.execute_reply":"2026-03-29T16:19:38.918576Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"## 5. Execution Pipeline\nIn this block, we prepare the metadata, create a simple train/validation split (you should upgrade this to StratifiedKFold later for better local validation), and launch the training engine.","metadata":{}},{"cell_type":"code","source":"# Define paths based on Kaggle environment\nDATA_DIR = \"/kaggle/input/competitions/birdclef-2026\" ","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.920392Z","iopub.execute_input":"2026-03-29T16:19:38.920668Z","iopub.status.idle":"2026-03-29T16:19:38.932784Z","shell.execute_reply.started":"2026-03-29T16:19:38.920641Z","shell.execute_reply":"2026-03-29T16:19:38.932071Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 1. Load Metadata\ntrain_metadata = pd.read_csv(f\"{DATA_DIR}/train.csv\")\n\n# 2. Add Full Filepaths\ntrain_metadata['filepath'] = DATA_DIR + '/train_audio/' + train_metadata['filename']","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:38.934606Z","iopub.execute_input":"2026-03-29T16:19:38.934965Z","iopub.status.idle":"2026-03-29T16:19:39.154165Z","shell.execute_reply.started":"2026-03-29T16:19:38.934937Z","shell.execute_reply":"2026-03-29T16:19:39.15355Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":" # Create target mapping if 'label_id' is missing\nif 'label_id' not in train_metadata.columns:\n    unique_birds = sorted(train_metadata['primary_label'].unique())\n    bird2id = {bird: idx for idx, bird in enumerate(unique_birds)}\n    train_metadata['label_id'] = train_metadata['primary_label'].map(bird2id)\n    \n    # Update config with correct number of classes\n    CFG.num_classes = len(unique_birds)\n    print(f\"Found {CFG.num_classes} unique species.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:39.155037Z","iopub.execute_input":"2026-03-29T16:19:39.155349Z","iopub.status.idle":"2026-03-29T16:19:39.168349Z","shell.execute_reply.started":"2026-03-29T16:19:39.155328Z","shell.execute_reply":"2026-03-29T16:19:39.167502Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 3. Simple Train/Valid Split (80/20)\n# Hint: Replace with StratifiedGroupKFold grouped by 'author' for a more robust setup\ntrain_df = train_metadata.sample(frac=0.8, random_state=CFG.seed).copy()\nvalid_df = train_metadata.drop(train_df.index).copy()\nprint(f\"Train samples: {len(train_df)} | Valid samples: {len(valid_df)}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:19:39.169351Z","iopub.execute_input":"2026-03-29T16:19:39.169943Z","iopub.status.idle":"2026-03-29T16:19:39.209207Z","shell.execute_reply.started":"2026-03-29T16:19:39.169921Z","shell.execute_reply":"2026-03-29T16:19:39.208524Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":" # 4. Launch Training\nrun_training(train_df, valid_df, CFG())","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-03-29T16:27:31.635362Z","iopub.execute_input":"2026-03-29T16:27:31.636109Z","iopub.status.idle":"2026-03-29T17:00:00.166764Z","shell.execute_reply.started":"2026-03-29T16:27:31.636067Z","shell.execute_reply":"2026-03-29T17:00:00.165708Z"}},"outputs":[],"execution_count":null}]}